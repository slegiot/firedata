[build]
builder = "DOCKERFILE"
dockerfilePath = "services/sports-service/Dockerfile"
watchPatterns = ["packages/**", "services/sports-service/**", "tsconfig.base.json", "pnpm-lock.yaml"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
