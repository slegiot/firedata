[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm run build --filter @firedata/finance-service"
watchPatterns = ["packages/**", "services/finance-service/**", "tsconfig.base.json"]

[deploy]
startCommand = "node services/finance-service/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
