{
  "name": "@firedata/news-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "fast-xml-parser": "^4.5.1",
    "@firedata/shared-types": "workspace:*",
    "@firedata/shared-cache": "workspace:*",
    "@firedata/shared-db": "workspace:*",
    "@firedata/shared-firecrawl-client": "workspace:*",
    "@firedata/shared-logging": "workspace:*",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.4"
  }
}
