[build]
builder = "DOCKERFILE"
dockerfilePath = "services/betting-odds-service/Dockerfile"
watchPatterns = ["packages/**", "services/betting-odds-service/**", "tsconfig.base.json", "pnpm-lock.yaml"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
