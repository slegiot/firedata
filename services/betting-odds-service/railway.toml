[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && npx turbo build --filter=@firedata/betting-odds-service..."
watchPatterns = ["packages/**", "services/betting-odds-service/**", "tsconfig.base.json"]

[deploy]
startCommand = "npx tsx services/betting-odds-service/src/server.ts"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
