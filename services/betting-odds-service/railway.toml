[build]
builder = "dockerfile"
dockerfilePath = "services/betting-odds-service/Dockerfile"

[deploy]
startCommand = "node services/betting-odds-service/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
