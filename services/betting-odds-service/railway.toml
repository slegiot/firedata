[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm run build --filter @firedata/betting-odds-service"
watchPatterns = ["packages/**", "services/betting-odds-service/**", "tsconfig.base.json"]

[deploy]
startCommand = "node services/betting-odds-service/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
